import XCTest
@testable import ContentsTransformer

final class ContentsTransformerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ContentsTransformer().text, "Hello, World!")
    }
}
